# Claim C6 — influence maximization

The producer is `repro/verify.py::verify_claim6`, using `repro/bfm/cpp_runner.py`, the author-provided implementation under `cpp_im/`, and the tracked result `outputs/cpp/cpp_slashdot.json`. The current committed audit checks ten Slashdot budgets, all of which beat the strongest tracked baseline; the mean factor is `4.564×`. Status: **PARTIAL DATASET PASS**. Email and Epinions faithful outputs, and therefore the paper’s three-dataset aggregate, remain outside the current evidence.
